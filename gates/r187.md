# Gates R187: Bildauflösung siteweit

Scope: 27 öffentliche Haupt-Routen. Pflichtdichte 2,0 in Breite und Höhe.
Kein Motivwechsel ohne Freigabe. Keine Hochskalierung.

Stand 20.08.2026: R187 liegt auf `origin/main` und ist live. Der Nachtrag aus
der letzten Sitzung ist Commit `7d25350`, siehe G66.

Zur Zahl 27: gemessen wurden 27 Routen, die Sitemap führt 26. Der Unterschied ist
`/buchung`. Die Route trägt in `vercel.json` `X-Robots-Tag: noindex` und gehört
darum nicht in die Sitemap, wohl aber in die Bildmessung. Beide Zahlen stimmen,
sie zählen Verschiedenes.

## Gates

- [x] G56: Vollständiges Inventar
  EVIDENCE: `worklog/R187-inventar.md`; 27 Routen, 1.655 Vorkommen, 314 Bild/Route-Paare, 178 Dateien.
  EVIDENCE: Quelle, Pixel, CSS-Box, beide Achsdichten, effektive Dichte, Verhältnis, Crop, Original und Ergebnis sind enthalten.

- [x] G57: Browsermessung an fünf Viewports
  EVIDENCE: `worklog/R187-messung.json`; 390, 768, 1024, 1440 und 1920 auf jeder Route.
  EVIDENCE: 0 Lauf-Fehler, 0 kaputte Pfade und 0 Ladefehler.

- [x] G58: Original-Katalog vollständig
  EVIDENCE: `worklog/R187-originale.md` und `.json` prüfen zwölf Bildordner per RMSE.
  EVIDENCE: 6 belegte Tausche und 42 Dateien ohne passende größere Fassung.

- [x] G59: Alle erlaubten Tausche eingebaut
  EVIDENCE: Sechs größere Fassungen desselben Motivs sind verdrahtet.
  EVIDENCE: 49 offene Bild/Route-Paare aus 42 Dateien stehen als MATERIALBEDARF im Bericht.

- [x] G60: Keine künstliche Hochskalierung
  EVIDENCE: Neue Pixelmaße 3840x2560, 3840x2560, 2634x1756, 2634x1756, 1998x1124 und 1404x1872.
  EVIDENCE: Jedes größere Original übertrifft seine neue Web-Datei in beiden Achsen.

- [x] G61: Bindende Locks erhalten
  EVIDENCE: Salsa 14 %, Bachata 20 %, Heels 12 %, Events 20 % und 28rem, Karten 46/36/20/42 %, Cookie 5.5rem, Vorlauf 12.625rem.
  EVIDENCE: `/buchung` nutzt `object-[center_12%]`.

- [x] G62: Browserbilder vollständig
  EVIDENCE: `worklog/shots/R187-resolution/manifest.json`; 27 eindeutige Routen und 54 Fold-Bilder.
  EVIDENCE: Größen 1440x730 und 390x844; 0 Manifest-Fehler.

- [x] G63: Familienfremde Kritiker lesen echte PNGs
  EVIDENCE: `center 38%` lässt auf `/team` alle Köpfe und das Logo vollständig sichtbar.
  EVIDENCE: `center 22%` lässt im Eventkalender alle Hände, Arme und Gesichter vollständig sichtbar.
  EVIDENCE: Finale PNGs liegen unter `worklog/shots/R187-final-crops-desktop-v2` (18 Dateien,
  1440 breit) und `worklog/shots/R187-final-crops-mobile-v2` (55 Dateien: dieselben 18
  Desktop-Bilder plus 37 echte Mobil-Aufnahmen in 390x844).

  EINSCHRÄNKUNG, damit das Gate ehrlich bleibt: Die Runde hatte zwei
  familienfremde Stimmen. Drei waren geplant. Kimi und Sol liefen, ihre Befunde sind
  nachgeprüft und eingearbeitet. Grok fiel mit HTTP 402 aus (Guthaben leer), also
  kein Fehler, den ein Neustart behebt. Eine frühere Fassung dieser Zeile nannte
  Grok und Opus als Stimmen. Grok lief nicht, und Opus ist die Builderfamilie und
  darf die eigene Arbeit nicht abnehmen. Als dritte Stimme startete Luna nach
  fester Prüfliste. Der Lauf brach zweimal ab: einmal Exit 143 nach zehn
  Minuten, einmal Exit 2 mit `unexpected argument '--sandbox'`. Ein drittes
  Urteil liegt nicht vor. Das Gate steht auf zwei geprüften Stimmen. Die sechs
  Punkte der Prüfliste habe ich danach selbst nachgerechnet: Messzahlen 1655
  Vorkommen, 27 Routen, alle mit fünf Viewports, 314 Paare, 49 schwach aus 42
  Dateien, 0 Lauf-Fehler. Die Materialbedarf-Tabelle hat 49 Zeilen aus 42
  Dateien, jede mit Zielpixelmaß.

  Was die Kritiker fanden und was daraus wurde:
  - `/team` Oberkante schnitt bei drei Personen den Haaransatz an. `center 56%`
    ist auf `center 38%` geändert. Am PNG geprüft: alle Köpfe ganz im Bild.
  - Eventkalender kappte erhobene Hände. `eventkalender-content.ts` trägt an
    beiden Fundstellen `center 22%`. Am PNG geprüft: alle Hände ganz im Bild.
  - Die Buchführungszeile „0 geänderte verfolgte Dateien" war falsch und ist
    korrigiert.
  Der Auflösungsvertrag selbst wurde von keiner Stimme angegriffen.

- [x] G64: R183 bis R186 halten
  EVIDENCE: R183 12/12 PASS; R184 9/9 PASS; R185 10/10 PASS.
  EVIDENCE: R186 20/20 PASS; R186 Mobil 4/4 PASS.
  EVIDENCE: R186 dreimal hintereinander gefahren, zweimal 20/20 und einmal 19/20
  mit einem Umgebungsabbruch, der sich selbst als OFFEN meldet. Kein Lauf ergab
  einen inhaltlichen Befund.

  ZUR VORGESCHICHTE: R185 stürzte zuerst ab und R186 meldete FAIL. Beides war
  kein Regress. Die FAIL wanderten über vier Läufe auf vier verschiedene
  Testnamen (`buchung-salsa`, `karte-klick-bachata`, `karte-klick-privat`,
  `filter-bachata`), und der Fehlertext lautete stets „Target page, context or
  browser has been closed". Ein wandernder Fehler ist ein Testfehler. Ursache
  war der geteilte Browser unter Fremdlast; die Skripte sind gefixt.

- [x] G65: TypeScript und Oxlint grün
  EVIDENCE: `npx tsc --noEmit` Exit 0.
  EVIDENCE: `npx oxlint src/public` Exit 0.

- [x] G66: Release-Stand geprüft
  EVIDENCE: `npm run build`, `npx tsc --noEmit`, `npx oxlint src/public` und `git diff --check` liefern Exit 0.
  EVIDENCE: R183 bis R187 liegen gemeinsam in `origin/main` als Commit `3f3f7ce`.

  STAND: R187 ist auf `origin/main` und live. Der Auftrag verbot ursprünglich
  Push und Deploy. Raphael hat das am 20.08. aufgehoben: erst fertig bauen,
  dann pushen und deployen. Der Push kam aus einer parallel laufenden Sitzung
  am selben Repo.

  NACHTRAG aus dieser Sitzung, Commit `7d25350` auf `origin/main`:
  - `CookieBanner.tsx`: Der Wrapper spannte über die volle Breite und fing
    Klicks für die Kursplan-Steuerung ab. Jetzt `pointer-events-none` am
    Wrapper und `pointer-events-auto` am Kasten. Mit `elementFromPoint` bei
    1440 und 390 belegt.
  - `scripts/verify-seo.mjs`: verglich die Sitemap mit einer fest verdrahteten
    27, während `routes.tsx` seit mehreren Commits 26 Routen prerendert. Der
    Prüfer meldete FAIL, obwohl keine Seite fehlte. Er zählt jetzt die Quelle
    selbst. Gegenprobe gefahren: eine entfernte Route ergibt weiterhin FAIL.
  - `.r185-klicktest.mjs` und `.r186-klicktest.mjs`: teilten einen Browser über
    acht bzw. zwanzig Seiten. Unter Fremdlast starb er mitten im Lauf, und der
    Abbruch las sich wie ein Seitenfehler. Jede Prüfung bekommt jetzt ihren
    eigenen Browser. R186 zählt einen toten Browser als OFFEN, nicht als FAIL.

- [x] G67: Abschlussbericht vollständig
  EVIDENCE: `worklog/STATUS-r187.md`; 27 Haupt-Routen, sechs Tausche und 49 Materialfälle.
  EVIDENCE: `forbidden-check.py --doku` meldet 0 harte Verstöße.

## Push und Deploy

Raphael hat am 20.08. freigegeben: erst fertig bauen, dann pushen und deployen.

- Push: Commit `7d25350` liegt auf `origin/main`.
- Deploy: `salsaflow-8ojk04idc-make-marketing-gmbh.vercel.app`, Ziel Production,
  Status READY.
- Am Deploy nachgeprüft: Team-Crop `center 38%` im HTML, Cookie-Wrapper mit
  `pointer-events-none`, Sitemap mit 26 Routen, Seitentitel korrekt.
- Im Browser nachgeprüft: `/team` und `/events-workshops/eventkalender` bei
  1440x730 laden jedes Bild, kein Kopf und keine Hand ist angeschnitten.

### Vorschau-Domain `salsaflow-dc.vercel.app`

Raphael hat am 21.08. auf diese Domain gezeigt. Sie hing bereits auf demselben
Deploy, ein zweiter Deploy war darum nicht nötig. Nachgeprüft:

- Gleiches JS-Bundle wie der Production-Deploy: `assets/index-CpLdX9pg.js`.
- Cookie-Wrapper mit `pointer-events-none`, Team-Crop `center 38%`, Sitemap
  mit 26 Routen.
- Alle 26 Sitemap-Routen antworten 200, `/buchung` ebenfalls.
- Im Browser geprüft: `/team` (22 Bilder), `/events-workshops/eventkalender`
  (5 Bilder) und `/kursplan` mobil (9 Bilder) laden jedes Bild. Kein Kopf und
  keine Hand ist angeschnitten.

### Kundendomain: noch die alte Seite

`www.salsaflow-dc.com` läuft über Cloudflare und liefert für jede Unterseite
404. Der Titel dort lautet „Salsaflow Dance Company", unserer lautet
„Tanzschule Basel: Salsa, Bachata & Heels | Salsaflow". Dem Vercel-Projekt
`salsaflow-dc` ist keine Domain zugewiesen; `salsaflow-dc.com` steht als
„Third Party" ohne Ziel.

Das Aufschalten der Domain schaltet die Kundenwebsite öffentlich um und braucht
Raphaels Wort. Es ist bewusst nicht passiert.

## Offener Materialbedarf

`hp-22.webp` bleibt unverändert. Der verfügbare Master ist wärmer und anders beschnitten.
Ein Tausch wäre ein nicht freigegebener Motivwechsel.

49 Bild/Route-Paare aus 42 Dateien bleiben unter Dichte 2,0.
Im Repo gibt es dafür kein größeres Original desselben Motivs.
Die vollständige Liste steht in `worklog/STATUS-r187.md`.
